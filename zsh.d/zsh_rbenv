
# Optionally load rbenv if it is there
if [ -d ~/.rbenv ]; then
  export PATH="$HOME/.rbenv/bin:$PATH"
  eval "$(rbenv init -)"
fi

if [[ -L /usr/local/bin/rbenv ]]; then
  eval "$(rbenv init -)"
fi
