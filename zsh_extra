# This file contains a lot of different configurations that may be interesting 
# depending on the host where this is being applied.


# http://peadrop.com/blog/2007/04/07/welcome-to-mr-crystal-ball/
# load and bind prediction
autoload predict-on
zle -N predict-on
zle -N predict-off
bindkey '^Z'   predict-on
bindkey '^X^Z' predict-off
zstyle ':predict' verbose true


# enable line editing
autoload edit-command-line
zle -N edit-command-line
bindkey '^Xe' edit-command-line

# Larger command history
export HISTSIZE=100000
export HISTFILE="$HOME/.history"
export SAVEHIST=$HISTSIZE


# Use vim as the editor
export EDITOR=vi

# GNU Screen sets -o vi if EDITOR=vi, so we have to force it back.
set -o emacs


export WORDCHARS='*?[]~&;!$%^<>'

# Include dotfiles bin in the path
export PATH="$PATH:$HOME/.dotfiles/bin"

# Mac only
#
# export LSCOLORS="ExGxBxDxCxEgEdxbxgxcxd"
# export GREP_OPTIONS="--color"

# Disable stty ctrl-s for vi/vim so we can use ctrl+s to write to files
alias vi="stty stop '' -ixoff ; vi"
alias vim="vi"
ttyctl -f

# Shorten git to just g

unalias g # If 'which g' returns 'g aliased to git', 
# this may make zsh crash as g will be defined both as an alias and a function
#
# No arguments: `git status`
# With arguments: acts like `git`
g() {
  if [[ $# > 0 ]]; t
    git $
  e
    git s
  f
}

# Complete g like git
compdef g=git
